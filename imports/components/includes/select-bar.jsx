import React, { Component } from 'react';

export default class SelectBarComponent extends Component {
  render(){
   return (
     <div className="container select-bar">
      Pick from one of our featured guides below or use the search bar to filter by artist, city and category
     </div>
   );
  }
 }
